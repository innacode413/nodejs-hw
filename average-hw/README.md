# average-hw

Функція, яка приймає масив чисел і повертає їх середнє значення.

- `average.js` — реалізація (експортує `average`).
- `test.js` — приклади виклику.

```bash
node test.js
```

Порожній масив повертає `0`.
